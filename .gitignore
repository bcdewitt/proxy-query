# IMPORTANT - don't include sensitive information (like _auth in .npmrc)
.npmrc
.env

# Ignore third-party JavaScript files (we get these with npm install and the folder itself will get auto-generated, too)
node_modules/

# Ignore our build folder (rollup will auto-create the folder itself, too)
dist/

# Ignore any log files
npm-debug.log

# Ignore OS-generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
